export async function register() {
  // El motor de conocimiento se inicializa bajo demanda desde sus rutas.
  // Importarlo aquí también lo incluiría en Middleware/Edge, donde sus
  // dependencias de Node y PDF no están disponibles.
}
