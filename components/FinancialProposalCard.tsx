'use client';
import { useState } from 'react';
import type { FinancialProposal } from '../types/financialAuthority';
export function FinancialProposalCard({proposal,onAccept}:{proposal:FinancialProposal;onAccept:(proposal:FinancialProposal)=>void|Promise<void>}){const[working,setWorking]=useState(false),[error,setError]=useState('');return <section className="mt-6 max-w-[780px] rounded-3xl border border-[#D9FF00]/30 bg-[#111407] p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-[#D9FF00]">Detecté un posible costo</p><h3 className="mt-3 text-xl font-semibold">{proposal.concept}</h3><p className="mt-2 text-lg">{proposal.quantity} × {proposal.unitPrice.currency} {proposal.unitPrice.amount.toLocaleString('es-CO')} = <strong>{proposal.calculatedTotal.currency} {proposal.calculatedTotal.amount.toLocaleString('es-CO')}</strong></p><p className="mt-2 text-sm text-[#999]">Estado inicial: estimado. Confirmar la interpretación no aprueba esta línea.</p>{error?<p className="mt-3 text-sm text-red-300">{error}</p>:null}<button type="button" disabled={working} onClick={async()=>{setWorking(true);setError('');try{await onAccept(proposal)}catch(e){setError(e instanceof Error?e.message:'No fue posible agregar la línea.')}finally{setWorking(false)}}} className="mt-5 rounded-full bg-[#D9FF00] px-4 py-2 text-sm font-bold text-black disabled:opacity-50">{working?'Agregando…':'Agregar al presupuesto'}</button></section>}
