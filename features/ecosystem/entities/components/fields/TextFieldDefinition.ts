import { createInputFieldDefinition } from '../../factories/create-input-field-definition';

export const textFieldDefinition =
  createInputFieldDefinition({
    type: 'text',
    inputType: 'text',
  });